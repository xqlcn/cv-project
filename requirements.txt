# PyTorch: install the CUDA build that matches your system from https://pytorch.org
torch>=2.1.0
torchvision>=0.16.0

transformers>=4.38.0
huggingface_hub>=0.20.0
open-clip-torch>=2.24.0
accelerate>=0.27.0

hydra-core>=1.3.2
omegaconf>=2.3.0
pyyaml>=6.0.1

numpy>=1.24.0
Pillow>=10.0.0
tqdm>=4.66.0
scikit-learn>=1.3.0
matplotlib>=3.8.0
pandas>=2.0.0
pyarrow>=14.0.0

# Mesh loading + offscreen rendering (ModelNet40 / synthetic pipeline)
trimesh>=4.0.0
# PyPI pyrender 0.1.45 pins PyOpenGL==3.1.0 (invalid on Python 3.7+). Upstream master uses PyOpenGL~=3.1.0.
pyrender @ git+https://github.com/mmatl/pyrender.git
# Renamed SGIX async_ modules; stay on 3.1.x for pyrender compatibility.
PyOpenGL>=3.1.7,<3.2
networkx>=3.0
scipy>=1.10.0
# Optional speedups (often flaky on macOS / py3.9): pip install PyOpenGL_accelerate

# Notebooks (VS Code / Cursor: install "Jupyter" extension, then pick this venv as kernel)
jupyter>=1.0.0
ipykernel>=6.29.0
